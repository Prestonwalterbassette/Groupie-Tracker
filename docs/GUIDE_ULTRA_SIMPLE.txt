================================================================================
    📖 GUIDE ULTRA SIMPLE - GROUPIE TRACKERS POUR DÉBUTANTS
================================================================================

🎯 TU VAS COMPRENDRE CHAQUE LIGNE DE CODE EN 30 MINUTES !


================================================================================
PARTIE 1 : C'EST QUOI CE PROJET ? (EN 3 PHRASES)
================================================================================

1. Un site web qui affiche des vinyles d'artistes musicaux
2. Quand tu survoles un vinyle → il tourne et joue de la musique
3. Quand tu cliques → tu vois les infos de l'artiste


================================================================================
PARTIE 2 : COMMENT ÇA MARCHE ?
================================================================================

IMAGINE UN RESTAURANT 🍔
-----------------------

TOI = Le client (ton navigateur Chrome/Firefox)
SERVEUR GO = Le serveur qui prend ta commande
API EXTERNE = La cuisine qui prépare le plat

SCÉNARIO :
1. Tu demandes "Je veux voir Queen" (tu cliques sur un vinyle)
2. Le serveur GO va en cuisine et demande "Donnez-moi les infos de Queen"
3. La cuisine (API) prépare les infos et les donne au serveur
4. Le serveur GO te les apporte sur un plateau
5. Tu reçois les infos de Queen et tu peux les lire


POURQUOI UN SERVEUR GO ?
-----------------------

PROBLÈME : Les navigateurs web ont une règle de sécurité appelée "CORS"
→ Tu ne peux PAS aller directement chercher dans la cuisine (l'API externe)
→ Tu es bloqué à ta table

SOLUTION : Le serveur GO joue le rôle d'intermédiaire
→ Il a le droit d'aller en cuisine
→ Il te ramène ce que tu demandes


================================================================================
PARTIE 3 : LE CODE GO (main.go) - EXPLIQUÉ COMME SI TU AVAIS 5 ANS
================================================================================

LA FONCTION MAGIQUE : proxyAPI
------------------------------

C'est la fonction la plus importante du projet.
Elle fait office de "livreur" entre toi et l'API externe.


LIGNE 1 : CRÉER LA FONCTION
----------------------------

func proxyAPI(targetURL string) http.HandlerFunc {

EN FRANÇAIS SIMPLE :
"Je crée une fonction appelée proxyAPI"
"Elle prend une adresse web (targetURL) en entrée"
"Elle retourne une fonction qui gère les demandes HTTP"

ANALOGIE :
C'est comme créer une recette de cuisine qui dit :
"Voici comment aller chercher les ingrédients à l'adresse X"


LIGNE 2 : RETOURNER UNE FONCTION
---------------------------------

return func(w http.ResponseWriter, r *http.Request) {

EN FRANÇAIS SIMPLE :
"Je retourne une fonction qui reçoit :"
→ "w" = un stylo pour écrire la réponse
→ "r" = la lettre de demande reçue

ANALOGIE :
Tu reçois une lettre (r) avec une question
Tu as un stylo (w) pour écrire la réponse


LIGNE 3 : ALLER CHERCHER LES DONNÉES
-------------------------------------

resp, err := http.Get(targetURL)

EN FRANÇAIS SIMPLE :
"Va chercher les données à l'adresse targetURL"
"Mets la réponse dans 'resp'"
"Si erreur, mets-la dans 'err'"

ANALOGIE :
Tu envoies ton ami chercher du pain à la boulangerie
→ resp = le pain qu'il ramène
→ err = s'il te dit "la boulangerie est fermée"

EXEMPLE CONCRET :
Si targetURL = "https://groupietrackers.../api/artists"
Alors Go va récupérer la liste de tous les artistes


LIGNE 4 : VÉRIFIER SI ÇA A MARCHÉ
----------------------------------

if err != nil {
    http.Error(w, "API unavailable", http.StatusServiceUnavailable)
    return
}

EN FRANÇAIS SIMPLE :
"Si err n'est PAS vide (donc il y a une erreur)"
"Alors écris 'API unavailable' au navigateur"
"Et arrête tout (return)"

ANALOGIE :
Si ton ami revient sans pain et dit "la boulangerie est fermée",
tu dis à ta maman "Désolé, pas de pain aujourd'hui"


LIGNE 5 : FERMER LA CONNEXION
------------------------------

defer resp.Body.Close()

EN FRANÇAIS SIMPLE :
"Quand tu auras fini TOUT le travail, ferme la connexion"
"defer = repousse à la fin"

ANALOGIE :
Tu mets un post-it sur ton frigo : "Éteins la lumière en sortant"
Même si tu oublies, le post-it te le rappellera


LIGNE 6-9 : COPIER LES INFORMATIONS
------------------------------------

for key, values := range resp.Header {
    for _, value := range values {
        w.Header().Add(key, value)
    }
}

EN FRANÇAIS SIMPLE :
"Pour chaque étiquette sur le colis reçu (resp.Header)"
"Copie cette étiquette sur le nouveau colis (w.Header)"

ANALOGIE :
Tu reçois un cadeau avec plein d'autocollants dessus
Tu les recopies tous sur le nouveau cadeau que tu vas offrir


LIGNE 10-11 : AJOUTER LA PERMISSION CORS
-----------------------------------------

w.Header().Set("Access-Control-Allow-Origin", "*")
w.Header().Set("Content-Type", "application/json")

EN FRANÇAIS SIMPLE :
"Ajoute une étiquette spéciale : 'Tout le monde peut lire ça'"
"Ajoute aussi : 'Le contenu est du format JSON'"

ANALOGIE :
Tu mets un panneau sur ta boîte aux lettres :
"Tout le monde peut prendre le courrier" (Access-Control-Allow-Origin)
"C'est du courrier en format JSON" (Content-Type)

POURQUOI C'EST IMPORTANT :
Sans cette étiquette, le navigateur refuserait de lire les données (CORS)


LIGNE 12-13 : COPIER LE CONTENU
--------------------------------

w.WriteHeader(resp.StatusCode)
io.Copy(w, resp.Body)

EN FRANÇAIS SIMPLE :
"Copie le code de statut (200 = OK, 404 = pas trouvé)"
"Copie tout le contenu de la réponse"

ANALOGIE :
Tu écris sur l'enveloppe : "Statut : OK ✓"
Puis tu copies le contenu de la lettre que tu as reçue

RÉSULTAT :
Le navigateur reçoit exactement les mêmes données que l'API externe,
mais avec la permission CORS ajoutée !


SCHÉMA COMPLET
--------------

Navigateur          Serveur GO              API Externe
    |                   |                       |
    |--[Demande]------> |                       |
    |                   |                       |
    |                   |--[Va chercher]------> |
    |                   |                       |
    |                   | <---[Renvoie JSON]----| 
    |                   |                       |
    |                   | (Ajoute CORS)         |
    |                   |                       |
    | <--[Renvoie]------                        |
    |                                           |


================================================================================
PARTIE 4 : LE JAVASCRIPT (ui.js) - LIGNE PAR LIGNE
================================================================================

ÉTAPE 1 : ATTENDRE QUE LA PAGE SOIT PRÊTE
------------------------------------------

document.addEventListener('DOMContentLoaded', function () {

EN FRANÇAIS SIMPLE :
"Quand la page web est complètement chargée, fais ceci..."

ANALOGIE :
Attends que le four soit chaud avant de mettre le gâteau dedans

POURQUOI :
Si tu lances le code trop tôt, les éléments HTML n'existent pas encore


ÉTAPE 2 : CRÉER LE BOUTON HAMBURGER 🍔
---------------------------------------

var toggle = document.createElement('button');

EN FRANÇAIS SIMPLE :
"Crée un bouton HTML"

ANALOGIE :
Prends une feuille de papier et dessine un bouton

toggle.textContent = '\u2630';

EN FRANÇAIS SIMPLE :
"Écris le symbole ☰ dans le bouton"

POURQUOI :
Le symbole ☰ ressemble à un hamburger (3 lignes)
C'est le symbole universel pour "menu"


ÉTAPE 3 : FONCTION QUI ADAPTE LE MENU
--------------------------------------

function updateNavVisibility() {
    if (window.innerWidth < 700) {
        nav.style.display = 'none';
    } else {
        nav.style.display = 'flex';
    }
}

EN FRANÇAIS SIMPLE :
"Si la largeur de la fenêtre est moins de 700 pixels (téléphone)"
→ "Cache le menu (on l'ouvrira avec le bouton hamburger)"
"Sinon (ordinateur)"
→ "Affiche le menu normalement"

ANALOGIE :
Si tu es dans une petite pièce, range le meuble
Si tu es dans une grande pièce, laisse le meuble dehors


ÉTAPE 4 : ÉCOUTER LE CLIC SUR LE BOUTON
----------------------------------------

toggle.addEventListener('click', function () {
    if (showing) {
        nav.style.display = 'none';
    } else {
        nav.style.display = 'flex';
    }
});

EN FRANÇAIS SIMPLE :
"Quand on clique sur le bouton hamburger"
"Si le menu est affiché → cache-le"
"Sinon → affiche-le"

ANALOGIE :
Un interrupteur de lumière : clic = allumer, re-clic = éteindre


ÉTAPE 5 : CHARGER LES ARTISTES
-------------------------------

async function fetchArtists() {
    const res = await fetch('/api/artists-proxy');
    return res.json();
}

EN FRANÇAIS SIMPLE :
"Va chercher la liste des artistes à l'adresse '/api/artists-proxy'"
"ATTENDS la réponse (await)"
"Convertis la réponse en objet JavaScript (json)"

ANALOGIE :
Tu envoies une lettre pour demander la liste des artistes
Tu ATTENDS la réponse
Quand elle arrive, tu l'ouvres et tu lis le contenu

POURQUOI "async" et "await" :
fetch() prend du temps (quelques secondes)
"await" dit "attends que ça finisse avant de continuer"
"async" dit "cette fonction peut attendre"


ÉTAPE 6 : CRÉER UNE CARTE VINYLE
---------------------------------

function createVinylCard(artist) {
    const card = document.createElement('div');
    card.className = 'vinyl-card';
    
    const cover = document.createElement('img');
    cover.src = artist.image;
    
    card.appendChild(cover);
    return card;
}

EN FRANÇAIS SIMPLE :
"Créer un conteneur (div)"
"Lui donner la classe 'vinyl-card'"
"Créer une image"
"Mettre la photo de l'artiste"
"Ajouter l'image dans le conteneur"
"Retourner le conteneur"

ANALOGIE :
Tu fabriques une carte Pokémon :
1. Prendre un morceau de carton (div)
2. Coller une étiquette "Pokémon" dessus
3. Prendre une photo (img)
4. La coller sur le carton
5. Donner la carte terminée


ÉTAPE 7 : FAIRE TOURNER LE VINYLE AU SURVOL
--------------------------------------------

frame.addEventListener('mouseenter', () => {
    frame.classList.add('playing');
    playArtistPreview(artist.id, artist.name);
});

EN FRANÇAIS SIMPLE :
"Quand la souris ENTRE dans la zone du vinyle"
→ "Ajoute la classe 'playing' (qui active l'animation CSS)"
→ "Lance la fonction qui joue la musique"

ANALOGIE :
Quand tu passes ta main devant un capteur automatique
→ La porte s'ouvre (animation CSS)
→ Une musique se lance

CE QUI SE PASSE :
Le CSS contient une règle :
.playing { animation: rotation 2s infinite; }
Qui fait tourner le vinyle en boucle


ÉTAPE 8 : JOUER LA MUSIQUE
---------------------------

async function playArtistPreview(artistId, artistName) {
    // 1. Arrêter la musique précédente
    if (currentAudio) {
        currentAudio.pause();
    }
    
    // 2. Chercher la musique sur iTunes
    const url = `https://itunes.apple.com/search?term=${artistName}`;
    const res = await fetch(url);
    const data = await res.json();
    
    // 3. Prendre l'URL de l'aperçu
    const previewUrl = data.results[0].previewUrl;
    
    // 4. Créer un lecteur audio
    const audio = new Audio(previewUrl);
    
    // 5. Mettre le volume à 30%
    audio.volume = 0.3;
    
    // 6. Lancer la lecture
    audio.play();
}

EN FRANÇAIS SIMPLE :
"Si une musique joue déjà, arrête-la"
"Va sur iTunes et cherche l'artiste"
"Récupère l'URL de l'aperçu musical (30 secondes)"
"Crée un lecteur audio avec cette URL"
"Baisse le volume à 30% pour pas faire trop de bruit"
"Lance la musique"

ANALOGIE :
Tu as un lecteur CD
1. Si un CD joue, tu appuies sur STOP
2. Tu vas dans ta collection et tu cherches l'artiste
3. Tu trouves le CD avec un aperçu de 30 secondes
4. Tu mets le CD dans le lecteur
5. Tu baisses le volume
6. Tu appuies sur PLAY


================================================================================
PARTIE 5 : LA RECHERCHE (search.js)
================================================================================

ÉTAPE 1 : AFFICHER DES SUGGESTIONS
-----------------------------------

input.addEventListener('input', () => {
    const query = input.value.toLowerCase();
    const matches = allArtists.filter(a => 
        a.name.toLowerCase().includes(query)
    );
    showSuggestions(matches.slice(0, 5));
});

EN FRANÇAIS SIMPLE :
"Quand l'utilisateur tape dans le champ de recherche"
→ "Prendre ce qu'il a tapé et le mettre en minuscules"
→ "Chercher tous les artistes dont le nom contient ce texte"
→ "Afficher les 5 premiers résultats en suggestion"

EXEMPLE :
Tu tapes "que" dans le champ
→ query = "que"
→ matches = ["Queen", "Queensrÿche", "Queen of the Stone Age"]
→ On affiche ces 3 suggestions

ANALOGIE :
Google qui te propose des suggestions pendant que tu tapes


ÉTAPE 2 : FILTRER PAR STYLE (ROCK, POP, ETC.)
----------------------------------------------

if (filter === 'rock') {
    return /rock|metal|punk/.test(artist.name.toLowerCase());
}

EN FRANÇAIS SIMPLE :
"Si le filtre est 'rock'"
→ "Vérifie si le nom de l'artiste contient 'rock', 'metal' ou 'punk'"

EXPLICATION DU SYMBOLE /.../ :
C'est une "regex" (expression régulière)
/rock|metal|punk/ = cherche "rock" OU "metal" OU "punk"
Le "|" signifie "OU"

EXEMPLE :
Artist name = "Metallica"
→ Contient "metal" → VRAI → on le garde

Artist name = "Madonna"
→ Ne contient ni rock, ni metal, ni punk → FAUX → on le retire


================================================================================
PARTIE 6 : LA CARTE (geoloc.js)
================================================================================

ÉTAPE 1 : CRÉER UNE CARTE
--------------------------

const map = L.map('map');
map.setView([20, 0], 2);

EN FRANÇAIS SIMPLE :
"Créer une carte Leaflet dans l'élément HTML #map"
"Centrer la vue sur latitude 20, longitude 0, zoom 2"

ANALOGIE :
Tu ouvres Google Maps et tu zoomes pour voir le monde entier


ÉTAPE 2 : AJOUTER LES TUILES (FOND DE CARTE)
---------------------------------------------

L.tileLayer('https://.../tile.png').addTo(map);

EN FRANÇAIS SIMPLE :
"Charger les images de carte depuis OpenStreetMap"
"Les ajouter à la carte"

ANALOGIE :
Comme poser des morceaux de puzzle (les tuiles) pour former une carte


ÉTAPE 3 : CONVERTIR UN LIEU EN COORDONNÉES GPS
-----------------------------------------------

const url = `https://nominatim.../search?q=${lieu}`;
const res = await fetch(url);
const data = await res.json();
const lat = data[0].lat;
const lon = data[0].lon;

EN FRANÇAIS SIMPLE :
"Demander à Nominatim : où est situé ce lieu ?"
→ Exemple : "usa-new_york"
"Nominatim répond : latitude 40.7128, longitude -74.0060"

ANALOGIE :
Tu demandes à Google : "Où est la Tour Eiffel ?"
Google te répond : 48.8584° N, 2.2945° E


ÉTAPE 4 : PLACER UN MARQUEUR
-----------------------------

const marker = L.marker([lat, lon]).addTo(map);
marker.bindPopup("<h3>Concert ici</h3>");

EN FRANÇAIS SIMPLE :
"Créer une épingle aux coordonnées (lat, lon)"
"L'ajouter sur la carte"
"Quand on clique dessus, afficher un popup 'Concert ici'"

ANALOGIE :
Planter un drapeau sur une carte papier avec une petite note dessus


================================================================================
RÉSUMÉ FINAL - LES 5 CHOSES À RETENIR
================================================================================

1. SERVEUR GO = LIVREUR
   → Il va chercher les données pour toi
   → Il ajoute la permission CORS

2. ASYNC/AWAIT = ATTENDRE
   → fetch() prend du temps
   → await dit "attends que ça finisse"

3. addEventListener = ÉCOUTER
   → 'click' = clic de souris
   → 'mouseenter' = survol
   → 'input' = saisie clavier

4. classList.add('playing') = ACTIVER L'ANIMATION
   → Ajouter une classe CSS qui fait tourner le vinyle

5. LEAFLET = CARTE INTERACTIVE
   → L.map() = créer la carte
   → L.marker() = ajouter une épingle


================================================================================
EXERCICES POUR TESTER
================================================================================

EXERCICE 1 : Change le volume audio
→ Ouvre ui.js
→ Trouve : audio.volume = 0.3;
→ Change à : audio.volume = 0.5;
→ Teste : le volume sera à 50%

EXERCICE 2 : Change la couleur du vinyle
→ Ouvre style.css
→ Trouve : .vinyl-frame.playing
→ Ajoute : border: 3px solid red;

EXERCICE 3 : Change le texte du bouton hamburger
→ Ouvre ui.js
→ Trouve : toggle.textContent = '\u2630';
→ Change à : toggle.textContent = 'MENU';


================================================================================
FIN DU GUIDE - TU AS TOUT COMPRIS ! 🎉
================================================================================

Maintenant tu comprends comment fonctionne chaque partie du projet.
Tu es prêt à modifier le code et à l'améliorer !

Bon codage ! 🚀
